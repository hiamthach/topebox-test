import React from 'react';

import Button from '../Button/Button';

const Header = () => {
  return (
    <header>
      <Button outline={true} type="secondary">
        Header
      </Button>
    </header>
  );
};

export default Header;
